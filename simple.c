#include <stdio.h>
/**
 * main - calculates two integers and prints sum.
 * @m - first integer
 * @n - second integer
 * @sum - value of m and n when added.
 * Return: 0 on success.
 */

int main(void)
{
	int m = 8;
	int n = 10;
	int sum = (m + n);

	printf("sum is %d\n", sum);
	return (0);
}
